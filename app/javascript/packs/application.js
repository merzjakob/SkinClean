import "bootstrap";
import { activeButtons } from '../components/radio_buttons';
import { addMessage, refreshForm  } from '../components/radio_buttons';
activeButtons();
addMessage(reviewHTML);
refreshForm(innerHTML);
